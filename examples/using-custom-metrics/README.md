# Custom Metrics Example

This Kite example demonstrates the use of custom metrics through a simple HTTP server that creates and populate metrics.
Kite by default pushes metrics to port `2121` on `/metrics` endpoint.

### To run the example use the command below:
```console
go run main.go
```
